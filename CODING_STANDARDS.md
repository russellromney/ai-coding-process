# Moved

This file is retired. The live rules now live in one place: `~/.claude/CLAUDE.md`
(tracked in the private dotfiles repo).

Why: rules in a separate file only work if the agent reads them, and in practice it
often never did. The rules that survived are now in the always-loaded config, rewritten
against real session experience. The rest was ceremony and got cut.
